Continue the transaction-categorisation dataset work using HANDOVER.md in this
directory. You have no need for the prior chat history. Read HANDOVER.md and
STATE.json, follow the specified reading order, verify the checkout/source state,
and run the tested synthetic startup checks before editing.

Carlos wants independently labelled representative, unseen-input and unfamiliar-
merchant evaluation views, with permanent separation from training and selection,
while augmenting both hinge and transformer training for one more supervised
retrain. Use only the customer-linked Plaid pool. Anonymous-ID recovery is closed.

Your first assignment is B03-A in HANDOVER.md: implement and adversarially test the
local reservation/learning-claim protocol with strict types and a storage interface,
using synthetic inputs only. Reuse the existing canonical B02 contract. Do not
convert local preflight into real authority, weaken novelty/group protections,
invent historical completeness, apply cloud changes, reserve/label real examples,
retrain or score locked sets. Prepare a reviewable milestone with actual evidence.

Work in a dedicated clean app worktree on a codex/ branch based on the completed
handover commit from feat/ob-txn-categoriser-plan. Record the actual base and branch;
do not switch or reset the user's default checkout. If your new task starts on main,
prepare the isolated handover-based branch before editing. Preserve unrelated work.
Mirror adapters/docs in a dedicated research worktree based on its handover state;
do not push the existing research main history or integrate into the app PR branch
without coordinating the reviewed result.

Proceed autonomously with reversible implementation, tests and documentation until
B03-A is complete, then request protocol review with the changed files, passing
tests, limitations and next step. This review checkpoint is specified in the user-
requested handover to preserve quality across the lower-cost agent handoff. Keep
the broader dataset objective active in your progress record; B03-A is only its
first bounded milestone. Do not ask Carlos to repeat settled decisions or to provide
credentials irrelevant to synthetic work.
