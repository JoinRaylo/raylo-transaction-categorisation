# B03-A synthetic implementation

Status: **implemented locally; protocol review requested**. This bounded
milestone implements the local reservation/learning-claim semantics required
before any real candidate admission. It does not claim B03 completion.

## Result

The canonical implementation is
`lib/raylo-txncat/src/raylo_txncat/benchmark_authority.py`. It imports and
reuses the existing B02 `Subject`, `Membership`, `IndexSnapshot`, `Preflight`,
`Purpose`, `Projection` and canonical JSON/hash functions. The test double is
thread-safe and stores each new epoch as a canonical immutable synthetic index
object.

All 24 B03-A tests pass. They exercise both benchmark reservations and pending
learning/selection exposure claims through the same compare-and-commit path.
The tests use only generated bytes and opaque synthetic IDs; no private archive,
customer-linked Plaid row, locked set, model, label or cloud client was opened.

## Reproduction

From the dedicated app worktree:

```sh
PYTHONDONTWRITEBYTECODE=1 \
PYTHONPATH=/private/tmp/txncat-b03a-app-worktree/lib/raylo-txncat/src:/private/tmp/txncat-b03a-app-worktree/apps/ob-txn-categoriser/src \
/private/tmp/txncat-c2-env/bin/python -m pytest -q -m unit \
  lib/raylo-txncat/tests/test_benchmark_authority.py
```

The command passes with `24 passed`. The handover startup suite remains a
separate regression command; it passed 115 tests before this implementation.
Run Ruff with the root `pyproject.toml` on the changed Python files.

## Implemented versus pending

Implemented here:

- strict versioned object/reference, intent/proposal, preparation, operation,
  receipt, alias and contamination types;
- immutable object re-verification by generation, byte length and digest;
- fresh-snapshot B02 policy checks with recorded epoch/check hooks;
- atomic synthetic epoch CAS, exact operation-ID retry and changed-content
  rejection;
- reservation, learning and model-selection conflict handling;
- append-only alias propagation and contamination holds; and
- explicit invalid, reject, quarantine, stale-epoch and unavailable outcomes.

Still non-authorizing or pending:

- production GCS/Firestore resource and IAM design, authenticated receipts and
  cross-process race tests;
- complete source/alias/customer/account/input/family/legacy history;
- B04 enforcement at every training, enrichment, selection and promotion route;
- sampling eligibility, annotation pilot, independent labels, sealed access and
  any real benchmark membership; and
- hinge/transformer retraining, scoring, waterfall changes or deployment.

The next bounded action after review is a scoped production object/transaction
adapter and IAM plan. Do not apply it until the new cloud scope is reviewed.
