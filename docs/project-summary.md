# Project summary — unified transaction categorisation

Plain-English overview of the whole project, for stakeholders and for re-orientation. Keep this updated as milestones land (add to §8 and the progress log at the bottom).

## 1. The problem we started with

Raylo's live credit-risk model reads Plaid's category names directly. That's fragile: Plaid released a new version of its taxonomy (December 2025) *after* Raylo went live on it, and the same transaction can get a different label between versions — meaning our model's features could silently change meaning. We also have two data providers who categorise the same spending differently: **Equifax** (73M transactions of history, Jul 2022–Sep 2025, a one-time data dump — no new data coming) and **Plaid** (4.3M transactions, live since Aug 2025). The goal: a transaction taxonomy **Raylo owns**, where provider categories are evidence feeding our definitions rather than being the definitions.

## 2. Understanding the data

Before building anything, we profiled both sources and found the differences run deep. Equifax's two category fields aren't a hierarchy at all — one says *what* the money was for, the other *how* it moved (the same "General Groceries" tag appears on supermarket shopping and on salary paid *by* a supermarket). And the two providers' merchant fields are different kinds of data: Equifax resolves every transaction to one of 6,518 clean, curated vendor names, while Plaid passes through 212,300 raw text strings — including people's names on bank transfers.

## 3. Why we couldn't just map categories to categories

We tested a direct category-to-category mapping and it failed in a predictable way: clean categories map cleanly (taxis, flights, gambling), but the catch-all buckets — which hold a huge share of volume — don't map at all. Worse, when we applied each provider's mapping to the ~2,300 merchants that appear in both datasets, they gave **different answers for 72% of merchants**. The conclusion that shaped everything after: *the merchant, not the category, is the stable anchor across providers.*

## 4. Building our own taxonomy

We built a two-level taxonomy: **274 detailed categories rolling up into 29 general ones**, verified to cover 100% of both providers' category values. Cross-cutting concerns — is it essential spending, is it debt, is it a priority debt (UK debt-advice concept), is it age-restricted — are separate flags rather than extra hierarchy, because those groupings aren't tree-shaped. Design was evidence-driven, including one important self-correction: an early version deleted categories that didn't predict arrears, which was the wrong test — predictive power tells you where it's *safe to aggregate*, not which categories deserve to exist. We also fixed a fairness trap (rent and mortgage are both "essential", so homeowners and renters are measured comparably) and confirmed gambling subtypes must never be merged (merging destroys the signal).

## 5. How transactions get categorised

Every transaction runs down a **precedence waterfall**: direction-based rules first (gambling winnings ≠ stakes), then mechanism rules (salary is salary regardless of who pays it), then a **curated merchant dictionary that overrides both providers** — that override is what makes the taxonomy provider-independent — then regex rules, then the provider mapping as fallback, and finally an explicit "unclassified". Every transaction records *which* tier decided it, so any categorisation is explainable in plain terms — important for fair-lending defensibility. We then measured the hard limit: our dictionary covers 88% of Equifax's merchant volume but only ~48% of Plaid's, because of Plaid's long tail of raw strings. So merchant matching alone can't finish the job — some kind of classifier is essential, not optional.

## 6. Testing whether AI can label merchants

We ran a cheap decisive experiment before committing to a big build: two different LLMs (Claude Haiku 4.5 and Claude Sonnet 5) independently labelled the 2,307 merchants that appear in both providers, and we scored them against Equifax's labels. First results looked poor (~67%) — but digging in revealed the *scoring stick* was the problem: Equifax's "ground truth" is just its own merchant dictionary, with conventions ours deliberately disagrees with (it files Netflix under broadband, Boots under beauty). So we isolated the 376 merchants where **both models agreed with each other but not with Equifax** and put them to a human reviewer with full evidence per merchant (the raw bank statement text, direction of money, what each provider says).

## 7. Human adjudication settled it (GREEN LIGHT)

The human review found the models were right or acceptably right on ~70% of disputes, Equifax right on 16%, and 13% genuinely unanswerable from the merchant name alone (a payment "to Revolut" can be different things; Equifax's "Marks & Spencer" turned out to be M&S *credit card* payments while Plaid's is store spending). Corrected score: **96.1% accuracy at detailed level, 98.2% at general level — a green light** for LLM-assisted labelling. The review also produced three assets: 195 pre-approved dictionary entries, 49 merchants flagged as needing transaction-level rules, and a **1,563-merchant gold-standard evaluation set** — the fair benchmark for everything that comes next.

## 8. Where we are now

The four-field categoriser is built and resolved: an Equifax-trained baseline confirmed the predicted bias (69% head / 30% tail / 10% on Equifax-overruled merchants), so it was retrained on our own production labels instead (tail accuracy 30% → 57.5%). Three architectures baked off to a statistical three-way tie; adopted the bounded TF-IDF + logistic regression model on practical grounds (34× smaller, auditable, stable). In parallel, production vocabulary labelling has scaled through three tranches (top 5k → 20k → 50k unmatched Plaid strings), pushing an accepted, provenance-tracked category onto roughly 41%+ of *all* Plaid transactions, with the human-review burden shrinking as a share of volume each tranche.

Side-findings worth stakeholder attention independent of this project: every Plaid data pull requests only 90 days of history when it could ask for more (kills several model features structurally); rent detection has been fixed (R13 re-enabled) but the fix turned out to be a fair-lending/audit-trail win rather than a risk-model one — the actual IV impact on outcomes was flat.

**Since (2026-08-22/23):** ran a full benchmark of every labelling approach tried so far — frontier LLMs (Gemini 3.7 Flash, Claude Opus/Sonnet/Haiku), a fine-tuned Gemini 2.5 Flash endpoint, our own locally fine-tuned small model, and two lightweight classifiers (TF-IDF, sentence embeddings) — all scored on the same clean, held-out set. Gemini 3.7 Flash leads clearly (84.2% leaf / 90.9% general, averaged over 3 runs since Gemini isn't fully deterministic). Also found and fixed a real bug that had let the labelling prompt balloon to 2.4 million characters, and rigorously tested (and rejected) two attempts to shrink it — the full set of 375 worked examples turns out to be teaching genuine transferable reasoning, not acting as a lookup table, so it stays. Next up: finish swapping Gemini 3.7 Flash into the live labelling pipeline in place of Haiku (decision made, implementation not yet started), and score the newly-built `gold_transactions_v4_slm_volume` gold set against all of the above.

---

## Progress log

- **2026-08-23 (evaluation methodology overhaul — locked test set, risk-category gold set, standing confusion tool)** — Compared our eval approach against Bud's published transaction-categorisation testing methodology and adopted three changes. (1) **A genuinely locked test set**: realised every existing gold set has already been used to pick a winner at least once (the SLM holdout set alone decided a 5-way prompt-compression test AND the Gemini-vs-Opus-vs-Sonnet-vs-Haiku model choice AND confirmed the resulting consensus design) — that's the classic multiple-comparisons trap, where a set's number quietly stops reflecting true generalisation the more decisions get made against it. Built `src/build_gold_v5_locked.py`: samples genuinely novel merchants (excludes all 52,912 merchants this project has ever labelled anywhere), with an explicit rule that it's never scored against anything until the actual go/no-go decision. 1,100 rows fetched and LLM-drafted, human review pending. (2) **A bespoke stratified gold set for the categories that matter most.** Built a confusion-matrix tool (`src/confusion_analysis.py`) and immediately ran it against the existing v4 predictions — it found that `financial_services_other` scores 100% wrong across all three frontier models (Gemini, Sonnet, Opus) and `gambling_unspecified` is systematically confused with `gambling_betting` in all three, both completely invisible behind 80-88% aggregate accuracy, because volume-weighted sampling only ever gave these leaves 3-4 rows to begin with. Built `src/build_gold_risk_categories.py`: 711 rows deliberately stratified ~20-per-leaf across all 34 gambling/BNPL/high-cost-credit leaves (not the housing/utility priority-debt leaves, which volume-weighted sampling already covers fine), fetched and LLM-drafted, human review pending. (3) **A standing process, not a one-off fix**: `confusion_analysis.py` is meant to run against every future benchmark and production tranche, with any risk-category leaf falling below a minimum bar becoming a required fix, not a footnote — the same "found it, fixed the real cause" pattern the Tesco/Tesco Bank bug followed, now made a repeatable habit instead of ad hoc firefighting. Full design in CLAUDE.md §12.

- **2026-08-23 (Tesco/Tesco Bank bug fixed)** — Fixed the merchant-collision bug logged below. Added a narrative-based override to `sql/apply_crosswalk.sql`, symmetrically on both the Equifax and Plaid branches: when the merchant string is bare `tesco` AND the raw narrative contains "tesco bank", resolve to `financial_institution_unspecified` instead of letting the T4 dictionary's `tesco -> groceries` match win. This had to be placed in the **T2 compound-rule tier** rather than T5 (deterministic rules) — T5 rules run *after* the T4 dictionary check in the CASE precedence, so a T5-tier fix would never have fired; the merchant dictionary would always win first. Verified end-to-end on live BigQuery data: the rule now correctly splits `merchant_name='tesco'` into 1,149 transactions resolving to `financial_institution_unspecified` (narrative-confirmed Tesco Bank) vs. 75,253 unchanged `groceries` (genuine supermarket purchases) — matching the 1,146/1,155 finding from the original investigation almost exactly. Full crosswalk SQL re-validated with a BigQuery dry run before and after the change. Also surfaced a separate, out-of-scope observation while fixing this: `src/generate_crosswalk_sql.py` wires every `merchant_dictionary.csv` row into the live crosswalk regardless of its `status` column (`pending`/`approved`/`llm_proposed` are not distinguished) — the dictionary's own `tesco bank -> financial_institution_unspecified` entry (status `pending`) is already live but was dead code for this bug anyway, since Plaid never emits that exact merchant string. Not acted on; flagging for a future pass.

- **2026-08-23 (Tesco/Tesco Bank merchant-collision bug found and logged)** — Caught while fact-checking a slide example, not through the normal eval pipeline: our merchant dictionary maps `tesco` (merchant name only) to `groceries`, but Plaid's own merchant-name resolution collapses "Tesco Bank" transactions down to the same bare string "Tesco" that the supermarket uses. Measured: within Plaid's `TRANSFER_OUT_OTHER`/`TRANSFER_IN_OTHER` buckets specifically, `tesco` appears 1,155 times, and 1,146 of those (99.2%) are narrative-confirmed Tesco Bank credit-card/direct-debit payments, not supermarket purchases — currently all silently mislabelled `groceries`. Scale is small (~1,150 of 4.28M total Plaid transactions, ~0.03%), but it's a systematic, wrong-direction miscategorisation (a debt repayment counted as grocery spend) for the population it hits — structurally identical to the already-solved Marks & Spencer / M&S Bank case (§4). Not yet fixed: needs a narrative-based override rule (same T1/T5 pattern as the existing direction rules), since the merchant-name field alone can't disambiguate the two entities once Plaid has already collapsed the distinguishing text out of it. A `tesco bank -> financial_institution_unspecified` dictionary entry already exists but is `pending`/unapproved *and* wouldn't fire anyway, since the join key is the bare merchant string, not the narrative.

- **2026-08-23 (Option 1 implemented — Gemini 3.7 Flash replaces Haiku in production labelling)** — Refactored `src/production_labelling.py`'s two-model consensus pair from Haiku+Sonnet to Gemini 3.7 Flash+Sonnet (Opus tiebreak unchanged), following through on the decision the gold-v4 scoring above confirmed. `run_labelling()` now dispatches to either an Anthropic tool-call path or a new Gemini path (numbered-index workaround for Gemini's >150-value enum limit). Backward compatibility was the main design constraint: tranches 1–3's archived, human-completed review workbooks use `haiku_leaf`/`haiku_correct` columns, and `apply_review()` now detects and accepts either naming so those files stay re-appliable after a future re-gate — verified directly against the real `data/production_review_tranche3_completed.xlsx` (118 `haiku_correct` verdicts route correctly). Smoke-tested the new Gemini path on 12 real, previously-fetched tranche-3 merchant strings (not a fresh BigQuery fetch, to avoid disturbing the live 50,000-string tranche-3 scratch state) — all 12 labelled with plausible categories and populated confidence. Not yet run on a full tranche end-to-end.

- **2026-08-23 (gold v4 scored — the production-population verdict, and it's good)** — Scored the new 900-row v4 gold set (a true random, volume-weighted sample of the *unmatched-Plaid* population — exactly the traffic our LLM tier serves) against the three candidate models plus the runtime classifier and Plaid's own categories. Headlines: **Gemini 3.7 Flash 88.3% leaf / 93.1% general; the proposed production consensus gate (Gemini + Sonnet agree, Opus tiebreak) accepts 96.4% of transactions at 89.9% / 93.3%; where Gemini and Sonnet simply agree (78.6% of rows) accuracy is 95.6%.** Plaid's native category manages only 16.9% leaf on this population. Two structural discoveries came out of the scoring: (1) 40.4% of this population is *already* covered by the expanded merchant dictionary, so the LLM tier's true serving population is the other ~60% — where the consensus gate still achieves 86.6% / 90.5% on accepted rows; (2) the cheap TF-IDF classifier, previously written off at 32% on the hard generalization holdout, scores **59.0%** on this volume-weighted set — real production traffic repeats the merchants it was trained on, making it a plausible instant-answer runtime tier after all. This confirms the Gemini-for-Haiku swap decision on the population that matters and clears the way for the production_labelling refactor. Full table and caveats: `data/gold_v4_scoring_report.md`.

- **2026-08-23 (Gemini 3.7 Flash 3-run average; full model comparison table finalised)** — Ran the Gemini 3.7 Flash benchmark three times end-to-end (needed because Gemini is not fully deterministic even at temperature 0 — see below) to get a proper average rather than trusting one run: **leaf 84.2% ± 0.15pp, general 90.9% ± 0.12pp** (individual runs: 84.4/84.2/84.1 leaf, 91.0/90.8/90.8 general). The tight spread across full 1,055-row runs, despite meaningful row-level flips between runs, means single-run benchmark numbers for this model are trustworthy in aggregate even though no individual prediction can be trusted to reproduce. This closes out the full model league table for the team meeting: **Gemini 3.7 Flash leads (84.2%/90.9%), ahead of Opus 5 (80.6–81.7%), Sonnet 5 (76.8–76.9%), Haiku 4.5 (72.9–73.7%), the Vertex-tuned Gemini 2.5 Flash (53.9%/61.6%, but by far the fastest and cheapest to run), our own fine-tuned local SLM (50.0%/59.5%), TF-IDF (32.0%/37.6%), sentence embeddings (27.6%/32.5%), and vanilla (non-fine-tuned) Gemma (0–17.7% depending on whether it's shown the taxonomy).** Carlos had already decided to swap Gemini 3.7 Flash in for Haiku in the production consensus pipeline (Gemini + Sonnet, Opus tiebreak) on the strength of the single-run numbers; this determinism check was requested specifically to validate that decision before implementing it, and it holds up. The refactor itself (`src/production_labelling.py`) is scoped but not yet started.

- **2026-08-22 (night) — Gemini 2.5 Flash fine-tuning: works, and is essentially free** — Investigated whether fine-tuning a first-party Gemini model (not just the open-weight local model) was feasible, at Carlos's request. It is: Google's Vertex AI supervised-tuning API takes the same shape as open-model tuning, just pointed at `gemini-2.5-flash`. Ran a 15-row smoke test (succeeded in 72 minutes) then the full 164,445-row training set (succeeded in 81 minutes). Full cost: **17.0M billable training tokens at ~$0.005/1M ≈ $0.085 total** — negligible. The resulting endpoint scores 53.9% leaf / 61.6% general on the standard eval — a real, working, learned model, but well behind every frontier LLM and even behind our own local fine-tune's best mode. Practical value is speed (20 rows/sec, ~13x faster than Gemini 3.7 Flash) and near-zero training cost, not accuracy — worth keeping in mind if a future use case trades accuracy for throughput/cost.

- **2026-08-22 (afternoon/evening) — the labelling-prompt saga: found a real bug, tested and rejected two "make it shorter" attempts, kept the full example set** — Investigating a question about whether provider categories were being used in the labelling prompt turned up something worse: the prompt had silently grown to 2.39 million characters. Root cause: a "note" field was actually a machine-generated provenance string ("LLM-consensus label, tier=...") from an earlier bulk-wiring step, not a human's disambiguation reasoning. Fixed, and added a guardrail so this specific failure mode can't silently recur. Separately, and this is the more important result: two different "make the prompt shorter" designs were built and rigorously tested (not just tried once and accepted) — a principles-only version, and a principles-plus-curated-examples version — both scored 2–4.5 percentage points *worse* than just keeping the full 375 worked examples, and the gap held (in fact widened) even on categories the examples don't directly cover, proving the examples teach transferable reasoning rather than acting as a lookup table. **Conclusion: the prompt stays long. Prompt caching already neutralises the cost concern; the actual risk was ungoverned growth, which is now guarded against.** Full history of what was tried and the exact numbers are preserved as a comment in the code (`src/build_tail_eval.py`) so this doesn't get re-litigated from scratch later.

- **2026-08-22 (day) — local SLM fine-tune completed; a genuine "less context is more" finding** — Finished the local Gemma fine-tune (164,960 training rows from the prior day's redesign): scanned 20 checkpoints and selected the best (checkpoint 38000, 50.0% leaf / 59.5% general on the standard eval, scoring by direct generation with no taxonomy shown at inference — the model already learned the mapping during training). Also benchmarked two lighter-weight alternatives on identical training data for comparison: TF-IDF + logistic regression (32.0%/37.6%) and a sentence-embedding classifier (27.6%/32.5%) — both clearly behind the fine-tuned SLM. Built genuine token-level constrained decoding (forces the model to only ever output a valid taxonomy leaf, never a hallucinated string) plus KV-cache prefix-priming to make "show the full taxonomy in the prompt" computationally tractable for a local model. Testing that combination surfaced an important, non-obvious result: showing the *fine-tuned* model the full taxonomy list at inference time makes it worse, not better (50.0% → 39.5% leaf) — it already learned the mapping directly and the extra list seems to distract it — while the same technique is essential for the *non-fine-tuned* model, taking it from ~0–3.5% up to 17.7%. **Practical takeaway: a fine-tuned small model should be deployed to generate directly, not prompted with the full category list.**

- **2026-08-22 (rent-rule R13 fix: real IV impact measured — flat, and why)** — Built a genuine before/after IV comparison against real credit-risk outcomes for the R13 rent-detection fix (joining Equifax transactions → `open_banking_transactions_with_matches` → `ds_first_order_proposal_pia_metrics`, excluding fuzzy `name_time` matches, 83,873-proposal cohort). Result: **IV essentially unchanged** (month3_1plus_pia 0.0086→0.0085, month12_1plus_pia 0.0068→0.0067, month12_3plus_pia 0.0071→0.0071 — all within noise). Reason found: R13 fixed 16,282 transaction-level misclassifications, but only 485 of 83,873 proposals (0.6%) had their rent-persistence count actually change (239 went from 0→1+ months) — most fixed transactions belong to proposals that already had rent detected in another month via the native crosswalk, so the proposal-level signal barely moves even though per-transaction accuracy improved a lot. Conclusion: keep the fix (better audit trail, better per-transaction truth for fair-lending defensibility), but it isn't a risk-model win by itself. Script: `src/rent_iv_analysis.py`; full report: `data/rent_iv_report.md`.

- **2026-08-21 (v3 volume-weighted evaluation complete — a major, positive finding)** — Carlos completed the v3 review (1,500 transactions, true random sample, no category stratification, so high-volume merchants dominate roughly as they do in production). Scored separately by provider (blending would need an arbitrary weight, and Plaid is what matters for live traffic): **Equifax native 71.8% leaf / 81.2% general vs our pipeline 82.0% / 87.4%; Plaid (the live system) native 32.5% leaf / 60.3% general vs our pipeline 63.4% / 74.2%.** This confirms the hypothesis discussed with Carlos: true volume-weighted accuracy is much higher than the v2 category-stratified breadth evaluation suggested (46.6% leaf overall), because real transaction volume concentrates heavily on well-known, dictionary-covered merchants — the breadth eval deliberately gave equal weight to rare and common categories, which understates real-world performance. **This is the number that answers "how good is this in practice today" — our pipeline nearly doubles Plaid's native-category accuracy on real production-shaped traffic.**

- **2026-08-21 (capacity planning + cascade re-measurement)** — Pulled real transaction-count-per-applicant stats from the live Plaid data (11,530 real 90-day Asset Reports) for future SLM capacity planning: mean 371, median 297, p90 741, p95 977, p99 1,548, max 5,199 — typically a few hundred transactions per application, occasionally into the low thousands. Re-measured the dictionary→classifier→LLM cascade after the T4 expansion below: dictionary+rules+mechanism tiers now resolve ~41.4% of Plaid transactions (up from ~33.8%) at an estimated 88-93% accuracy (blend of the original curated dictionary, ~93%, and the newly-added production-tranche entries, ~84.9%); the remaining ~58.6% still has no trustworthy automated answer, since the classifier's own confidence score is uncalibrated (flat ~30% accuracy regardless of confidence threshold) and can't safely gate which of its predictions to trust.

- **2026-08-21 (production-labelled vocabulary wired into the live dictionary)** — Investigated why the dictionary/rules pipeline only covers ~36.5% of a clean sample: the 50k-string production-labelled Plaid vocabulary (already validated at 82-91% for its trusted tiers) had been used for classifier/SLM training but never fed the live T4 dictionary. Added 18,019 new entries (auto_accept/accepted/human_reviewed only, excluding the weak accepted_tiebreak/accepted_general tiers and every Tier A merchant to avoid reintroducing the leakage-audit circularity). Dictionary: 806 → 18,825 entries. The clean eval holdout can't show this improvement by construction (it's drawn entirely from the excluded Tier A merchants) — measured instead on live BigQuery population data: **Plaid T4 dictionary coverage 30.5% → 39.1% (+8.6pp)**, as expected since the additions are Plaid-sourced; Equifax barely moves (+0.8pp). Separately measured that the classifier's own confidence score isn't calibrated (accuracy stays flat ~30% regardless of confidence threshold on the T6-fallback remainder) — it can't currently be used to safely gate which predictions to trust vs. route elsewhere.

- **2026-08-21 (SLM training set redesigned)** — Ran full readiness checks before building anything: found 76 of 275 leaves had under 5 combined examples across gold_transactions_v2 and production_labels_tranche3 (22 with zero), and 61 merchants where the two sources genuinely contradict (mostly mechanism-dependent Tier A facts, e.g. a specific refund, vs Tier B's blanket merchant-level label). Ran a targeted top-up (`src/build_tuning_leaf_topup.py`, same sourcing technique as batch 2, LLM-consensus only — no full human review needed for training bulk): recovered 11 of the 76 thin leaves; 65 remain thin, mostly genuine data rarity rather than a sourcing gap. Rebuilt `src/build_tuning_dataset.py` around an explicit tiered-trust philosophy: Tier A (gold_transactions_v2/v3, human-verified) supersedes Tier B (production_labels' trusted tiers only — excludes `accepted_tiebreak` at 66.9% and `accepted_general` at 33.3%) for any overlapping merchant; Tier A is split by *merchant* into training vs. a clean held-out eval set (`data/gold_v2_slm_eval_holdout.csv`, 456 merchants/544 txns, verified zero overlap with training); known-conflicting (context-dependent) merchants are always kept in training and oversampled 3x rather than held out, since that's precisely the transaction-level-discrimination signal worth teaching. Result: 164,960 training rows, 250/276 classes covered.

- **2026-08-21 (compounding the review effort)** — folded the gold_transactions_v2 hand review back into the pipeline, as agreed with Carlos, rather than treating it as a one-off eval. Two changes: (1) **271 new T4 dictionary entries** promoted from the gold set, with a deliberately conservative filter — excluded any merchant with conflicting labels across the gold set (57 found, e.g. `revolut`/`monzo`/`paypal` genuinely resolve differently by transaction), any generic payment-rail string, any mechanism-dependent leaf (`refund_received`, `salary`, `transfer_p2p`, etc. — those describe a specific transaction's direction, not a stable merchant property; naively adding `selfridges -> refund_received` would have wrongly overridden every normal Selfridges purchase), and required a reviewer note as evidence of genuine reasoning (the un-noted candidates were full of garbled reference strings and generic words that don't generalise). Dictionary grew 535 → 806 entries. Re-ran `score_v2` with a new anti-circularity guard (excludes any gold row whose dictionary entry was itself sourced from that same gold set, from the accuracy figures) — genuine, non-circular improvement: our-pipeline accuracy 46.6% → **50.1%** leaf on the same held-out methodology. (2) **375 dictionary notes wired into the LLM system prompt** as a "worked disambiguation examples" section (`load_example_notes()` / `build_notes_addendum()` in `gating_experiment.py`, wired into `production_labelling.py` and `build_tail_eval.py`) — future labelling runs now see real reasoning like "AFSL is Admiral Financial Services Ltd unsecured personal loans" rather than a bare merchant-to-leaf lookup, specifically targeting the Admiral-Insurance-vs-Admiral-Casino class of ambiguity Carlos flagged.

- **2026-08-21 (v2 evaluation, both batches complete — 3,000 transactions)** — Batch 2 (1,500 more transactions, 482 drafts overridden) is in, bringing the combined leakage-free gold set to `data/gold_transactions_v2.csv` + `data/gold_transactions_v2_batch2.csv` = 3,000 real, independently hand-reviewed transactions. Re-ran `score_v2`: **overall, native provider category 39.9% leaf / 55.2% general vs our pipeline 46.6% leaf / 58.5% general.** By population: broad random sample (typical traffic) 41.5% vs 46.2%; deliberately-hard previously-disputed merchants 10.7% vs 31.3%; batch 2's breadth-targeted rows (precise Equifax subcategory sourcing) score much higher, 65.4% vs 64.6%, as expected for cleanly-sourced examples. **70.1% of transactions still fall through to the native-crosswalk fallback** — dictionary coverage remains the clear biggest lever. Cross-referenced the SLM fine-tuning training data (`data/production_labels_tranche3.csv`, ~315k rows) against this clean gold set: the "accepted" tiers (82.5% of the vocabulary, never individually human-reviewed) score **75.0%** accuracy — decent, but with `accepted_tiebreak` (the single largest tier) notably weaker at 66.9%. Recommendation for the SLM fine-tune: down-weight/exclude `accepted_tiebreak`, fold in the new gold set (holding back a slice as honest post-tuning eval), and specifically construct training examples that teach transaction-level discrimination (the same merchant string with genuinely different correct labels depending on description/amount/direction — e.g. Admiral Insurance vs Admiral Casino) rather than the current merchant-level-label design, which structurally cannot teach this since `context_dependent` merchants are excluded from training entirely today.

- **2026-08-21 (v2 evaluation, batch 1 complete)** — Carlos built and fully hand-reviewed a genuinely clean transaction-level gold set (1,500 real transactions, `data/gold_transactions_v2.csv`) to replace the leaky merchant-level one: 403/1,500 (27%) of the LLM-drafted proposals were overridden, including 34 of the 400 rows that already had a prior human verdict from earlier work — a real, from-scratch review, not a rubber stamp. Re-ran the three-way comparison against it (`src/final_evaluation.py score_v2` → `data/final_evaluation_v2_report.md`). **Headline numbers are much more modest than the earlier (biased-toward-famous-brands) evaluation, and this is the more trustworthy number**: overall, native provider category 32.1% leaf / 47.9% general vs our pipeline 40.9% leaf / 52.6% general. Split further: on the broad random sample (the closest thing to "typical incoming transaction" in this set), native 40.4% vs our pipeline 47.1%; on the deliberately-hard already-verified subset (merchants that needed adjudication in earlier work precisely because they were disputed), native crashes to 9.5% vs our pipeline 23.8%. **58.5% of real transactions still fall through to the native-crosswalk fallback** (not yet resolved by our own 535-entry dictionary/rules) — confirming, with much more rigour than before, that dictionary coverage is the single biggest lever left. The directional conclusion holds (our pipeline beats native categories in every cut), but the margin and absolute level are far more honest than the old 82–95% figures, which were inflated by sampling bias toward well-known merchants. A second 1,500-transaction batch (targeted at taxonomy leaves the first batch missed) is with Carlos for review; once merged in this becomes a combined ~3,000-transaction gold set.

- **2026-08-21 (leakage audit)** — Carlos asked for an explicit check that the final-evaluation numbers below weren't inflated by leakage before trusting them. Found two real issues, both now fixed in `src/final_evaluation.py` and `data/final_evaluation_report.md`: (1) **79.3% of the gold head set (1,239/1,563) was never human-reviewed** — it's `consensus_all_agree`, i.e. Haiku and Sonnet independently agreeing with each other, which is model self-consistency, not independent ground truth; only 20.7% (324 merchants) went through real human adjudication, and of those, 195 are directly circular (the same adjudication verdict both set the gold label AND added the merchant to the T4 dictionary). (2) **75% of the gold tail set (185/247) is directly circular** — `gold_leaf` was set to the exact haiku/sonnet prediction being scored (`build_tail_eval.py finalise()`), so scoring our pipeline against those rows is tautological; only `override`+`unclassifiable` rows (62/247) are genuine tests. Recomputed on the clean, non-circular subsets only (head n=129 restricted to `adjudicated_equifax` — a deliberately adversarial slice where a human said Equifax was right; tail n=62 restricted to override/unclassifiable — the hardest cases by construction): our pipeline still comes out ahead in most slices (e.g. head-via-Equifax-txn 62.0% vs Equifax-native 43.4%) but the margins shrink a lot and sample sizes are small, so these are stress-test/lower-bound numbers, not headline claims. Two findings are untouched by any of this and remain solid: the **provider-independence check** (our pipeline agrees with itself across Equifax vs Plaid transactions 51.2% of the time vs the known 27.8% crosswalk-only baseline — this never touches gold at all) and the **dictionary-coverage structural finding** (only 25.8% of gold head merchants are in T4 yet). Bottom line: the directional conclusion holds, but don't quote the full-sample 93.9%/95.0% figures as precise facts in front of stakeholders — present the clean-vs-full framing transparently instead.

- **2026-08-21 (later)** — **final three-way validation run: our pipeline vs trusting either provider's own category, both scored against the independent gold sets.** Built `src/final_evaluation.py` and `data/final_evaluation_report.md`. Headline: overall leaf accuracy is **93.9% for our pipeline vs 29.9% for Plaid's native category** (n=1,754 scored). Broken down: on the 1,563 head merchants (both providers), Equifax's own category alone gets 82.3% leaf / 93.7% general, Plaid's own gets only 31.9% / 63.5%; our pipeline gets 95.0% / 98.2% when run on Equifax transactions and 50.4% / 72.1% on Plaid transactions. On the 247 tail merchants (Plaid-only long tail), Plaid's own category is 17.0% leaf / 43.7% general; our two-model LLM consensus gets 84.8% / 90.6% on the 191 it's confident enough to answer (correctly abstaining on the rest rather than guessing). Root cause of the head-population Equifax/Plaid asymmetry: only 25.8% of gold head merchants are in the current 535-entry T4 dictionary yet — where they are, the pipeline is provider-independent (95.5% either way); where they aren't, Plaid-sourced accuracy falls back toward Plaid's own native 31.9%. Flagged one caveat: 214 of the 535 dictionary entries came from the same gating adjudication that produced the gold set, so the "in dictionary" figure is partly circular for that slice — doesn't affect the tail results or the overall provider-vs-provider comparison, which are the load-bearing numbers. **Conclusion: the work clearly outperforms trusting either provider's native categories, with dictionary coverage expansion identified as the single biggest remaining lever** — validated before starting any feature-engineering work (Experiment 3), per Carlos's explicit ask.

- **2026-08-21** — T4 (merchant dictionary) and T5 (deterministic rules) wired into `sql/apply_crosswalk.sql` for the first time. Merged the 195 gating-approved dictionary entries plus 19 evidence-backed context-dependent merchants into the dictionary (535 entries, up from 321) and added 3 new direction-based T5 rules. Measured real tier distribution on a BigQuery sample: **Equifax now resolves 34.8% of volume via the dictionary** (T4, was 0% before wiring) with 54.4% still falling to the provider-crosswalk fallback and 5.9% unclassified; **Plaid resolves 30.5% via the dictionary**, 66.1% via fallback. Deliberately left ~80 of the ~100 accumulated context-dependent merchants unresolved rather than forcing a guess — most are either same-direction product ambiguity (e.g. a building society could be mortgage or savings) or merchant-string normalisation collisions that genuinely merge unrelated businesses (e.g. `"water"` merging a Teemill order with a water-bill narrative). Also caught and fixed a real pre-existing bug: a T1 rule pointed at a leaf (`gambling_winnings`) that doesn't exist in the taxonomy, silently orphaning gambling credits — fixed to the existing `gambling_unspecified` leaf.

- **2026-08-20 (night)** — tranche 3 fully closed: 692 risk-boundary strings human-reviewed (opus correct in 388, haiku 118, sonnet 69, override 68, context_dependent 16, unsure 33 — rule backlog now 109+50=159 candidates). Final: **84.5% of tranche volume carries an accepted label, 15.4% deliberate abstention, only 0.1% left context-dependent/unresolved**. Labels snapshotted to `data/production_labels_tranche3.csv`, review workbook to `data/production_review_tranche3_completed.xlsx`. Across all three tranches combined, an accepted, provenance-tracked category now sits on the great majority of the 50,000 highest-volume unmatched Plaid strings (56.1% of unmatched volume), pushing overall Plaid transaction coverage to roughly 41%+.

- **2026-08-20 (evening)** — tranche 3 auto-labelling complete: top 50,000 unmatched Plaid strings (56.1% of unmatched volume) labelled and Opus-tiebroken, leaving **1,122 three-way disagreements (2.2%)**. After re-applying tranches 1–2's archived human reviews, the genuinely new human-review queue for tranche 3 is **692 strings (1.4% of tranche volume)** — awaiting human review, same as prior tranches. Also concluded the Gemini 3.7 Flash vs Opus 5 tiebreak comparison on the same disagreement queue (18,430 strings, both fully labelled): **73.6% leaf agreement, 82.7% general-category agreement** between the two models — Gemini measured at 2.8 strings/sec; Opus's own script doesn't log elapsed time, but wall-clock across the restarted runs was comparable. Notion doc and stakeholder slide outline updated with the full story (dual motivation, benchmark resolution, tranche progress, bake-off result).

- **2026-08-20 (afternoon)** — distillation model bake-off resolved: after two failed LightGBM attempts (class collapse, then training instability from unweighted-then-over-weighted class balancing), a third attempt (capped/dampened sample weights, lower learning rate, regularisation) reached genuine parity with the two linear models (51.6%/57.1% vs B's 53.4%/57.5% head/tail leaf) -- a three-way statistical tie, not a clear winner. **Adopted B (bounded TF-IDF + logistic regression)** on practical grounds: 34x smaller than the hashed-feature control, fully auditable coefficients, no training-stability risk. Also ran a side-by-side Gemini 3.7 Flash vs Opus 5 tiebreak comparison on tranche 3's disagreement queue (~18,400 strings) for speed/accuracy/cost -- in progress.

- **2026-08-20 (tranche 2 closed)** — vocabulary labelling extended to the top 20,000 unmatched Plaid strings (45.4% of unmatched volume, up from tranche 1's 30%). Same pipeline: Haiku + Sonnet enriched labelling on the ~15k new strings, Opus tiebreak over the disagreement queue (8,793 → 434 after tiebreak), 321 new risk-boundary strings human-reviewed (158 opus / 46 haiku / 34 sonnet correct, 49 override, 19 new context-dependent, 14 unsure, 1 unclassifiable, 2 abstain). Final: **~86.5% of tranche volume accepted, ~11.5% deliberate abstention, 35 context-dependent rule candidates** (rule backlog now 74+35=109). Snapshotted to `data/production_labels_tranche2.csv` and `data/production_review_tranche2_completed.xlsx`. Review-queue proportion held steady tranche-over-tranche (2.3% of volume both times), confirming the policy scales.

- **2026-08-20 (later)** — tranche 1 fully closed: three-way disagreements resolved by policy (general-consensus auto-accepts, benign ambiguity abstains, risk-boundary cases escalate) leaving just 113 strings for human review; all 113 adjudicated (Opus was the correct model in 52 of 72 model-pick verdicts). Final: **~89% of tranche volume carries an accepted label, ~10.6% deliberate abstention, 16 new context-dependent rule candidates**. Labels snapshotted to `data/production_labels_tranche1.csv`.

- **2026-08-20** — production vocabulary labelling, tranche 1: top 5,000 unmatched Plaid strings labelled by Haiku + Sonnet with transaction evidence, then a Claude Opus tiebreaker over the 2,784 strings the pair couldn't settle (2-of-3 majority, tiebreaker must be in the majority). Final gate: **85% of tranche volume carries an accepted label** (auto_accept 13.6% + accepted 32.4% + tiebreak 38.9%), 5.7% confirmed-abstain, only 9.4% (543 three-way disagreements) left for human review. Total LLM spend ~$12.

- **2026-08-19 (night)** — four-field ML baseline trained (1.79M Equifax transactions, char-n-gram + amount + direction linear model) and evaluated on both gold strata: **69% leaf on the head set, 30% leaf on the tail — far behind the enriched LLM (76% on tail)**. The breakdown confirms the predicted bias mechanically: on rows where the human ruled against Equifax's conventions, the Equifax-trained classifier scores 10%. Conclusion: the classifier cannot be distilled from Equifax labels; the LLM-consensus labels must be the distillation source. Vocabulary labelling via LLM is now clearly the primary route.

- **2026-08-17 and earlier** — data profiling, crosswalk rejection analysis, taxonomy v1–v3 (274 leaves / 29 generals, verified 0 uncovered provider values), merchant dictionary (321 entries), precedence waterfall design, feature-form and IV analyses.
- **2026-08-18** — gating experiment ran twice (Haiku 4.5, then methodology review + Sonnet 5 comparison). Discovered the ground truth is Equifax's own vendor dictionary; built cross-model consensus analysis; exported 376 disputes with per-row evidence to an adjudication workbook. Also verified both providers carry raw bank narratives (Equifax `Description`; Plaid `transaction_name`/`original_description`).
- **2026-08-19 (evening)** — tail evaluation set adjudicated: 260 strings sampled from the ~210k unmatched Plaid population (six strata), labelled by both models with transaction evidence, human-verified → `data/gold_tail_labels.csv` (247 gold labels + 9 more transaction-level rule candidates). First readout on the real deployment population: Sonnet 5 enriched hits 76% leaf / 83% general overall, 90% leaf on the top-volume stratum; hardest strata are two-word names and the uniform tail (~68–75%). Also surfaced: Plaid's own merchant entity resolution has errors (Iceland supermarket → "Icelandair"), so raw narratives outrank Plaid merchant names as classifier input.
- **2026-08-19 (later)** — taxonomy boundary conventions written into the leaf notes (groceries/convenience/discount/specialist, web vs online services, takeaway vs restaurant, lottery vs prize competitions, pharmacy vs beauty, insurance) and `marketplace_amazon` split: it is Amazon-only again, with a new `marketplace_general` leaf for eBay/Vinted/Depop/Etsy — preserving the Amazon-specific risk signal. Dictionary and gold set updated to match (275 leaves now). Repo put under git; human-verified assets moved to tracked `data/`.
- **2026-08-19** — human adjudication of all 376 disputes completed: **GREEN LIGHT, 96.1% corrected leaf accuracy / 98.2% general**. Produced the 1,563-merchant gold eval set, 195 approved dictionary entries, 49 transaction-level rule candidates. Four-field categoriser scoped as next stage.
